# 🍅 Pomodoro Telegram Bot

Telegram-бот для управления техникой Pomodoro, созданный с использованием aiogram 3.x.

## 📋 Описание

Этот бот помогает вам использовать технику Pomodoro для повышения продуктивности:
- 🍅 **Pomodoro сессии**: настраиваемая длительность работы
- ☕ **Короткие перерывы**: настраиваемый короткий перерыв
- 🌴 **Длинные перерывы**: настраиваемый длинный перерыв
- 🔄 **Полный цикл**: автоматический бесконечный цикл Pomodoro
- 📊 **Статистика**: отслеживание завершенных сессий
- ⚙️ **Настройка интервалов**: индивидуальная настройка длительности для каждого пользователя

## 🚀 Установка

### Локальная установка

1. Клонируйте репозиторий:
```bash
git clone https://github.com/xtzp0te/pomodoro.git
cd pomodoro
```

2. Установите зависимости:
```bash
pip install -r requirements.txt
```

3. Создайте файл `.env` на основе `env.example`:
```bash
cp env.example .env
```

4. Получите токен бота у [@BotFather](https://t.me/BotFather) в Telegram и добавьте его в файл `.env`:
```env
BOT_TOKEN=ваш_токен_бота
```

5. Запустите бота:
```bash
python main.py
```

### Docker Compose

1. Клонируйте репозиторий:
```bash
git clone https://github.com/xtzp0te/pomodoro.git
cd pomodoro
```

2. Создайте файл `.env`:
```bash
cp env.example .env
```

3. Отредактируйте `.env` и укажите токен бота и версию образа:
```env
BOT_TOKEN=ваш_токен_бота
IMAGE_TAG=latest
```

4. Запустите через Docker Compose:
```bash
docker-compose up -d
```

5. Просмотр логов:
```bash
docker-compose logs -f
```

6. Остановка:
```bash
docker-compose down
```

### Docker

1. Получите образ из GitHub Container Registry:
```bash
docker pull ghcr.io/xtzp0te/pomodoro:latest
```

2. Запустите контейнер:
```bash
docker run -d \
  --name pomodoro-bot \
  --restart unless-stopped \
  -e BOT_TOKEN=ваш_токен_бота \
  ghcr.io/xtzp0te/pomodoro:latest
```

## 🎮 Использование

1. Найдите вашего бота в Telegram и отправьте команду `/start`

2. Используйте кнопки для управления:
   - 🔄 **Запустить полный цикл Pomodoro** - запустить бесконечный цикл (Pomodoro → перерыв → Pomodoro...)
   - 🍅 **Настроить Pomodoro** - изменить длительность Pomodoro сессии
   - ☕ **Настроить короткий перерыв** - изменить длительность короткого перерыва
   - 🌴 **Настроить длинный перерыв** - изменить длительность длинного перерыва
   - 📊 **Статистика** - посмотреть вашу статистику
   - ⏹️ **Остановить таймер/цикл** - остановить текущий таймер или цикл

3. При настройке интервалов введите желаемое значение в секундах

4. Полный цикл автоматически чередует Pomodoro и перерывы:
   - После каждых 4 Pomodoro делается длинный перерыв
   - Между Pomodoro делаются короткие перерывы
   - Цикл работает до остановки пользователем

## 📝 Команды

- `/start` - Начать работу с ботом
- `/help` - Показать справку
- `/stats` - Показать статистику

## 💡 Техника Pomodoro

Классическая техника Pomodoro:
1. Работайте 25 минут (Pomodoro)
2. Сделайте 5-минутный перерыв
3. Повторите цикл
4. После каждых 4 Pomodoro сделайте 15-минутный перерыв

**Примечание**: В боте вы можете настроить любые интервалы по своему желанию!

## 🐳 Docker

### Образы

Образы доступны в GitHub Container Registry:
- `ghcr.io/xtzp0te/pomodoro:latest` - последняя версия
- `ghcr.io/xtzp0te/pomodoro:v1.0.0` - конкретная версия

### Лимиты ресурсов

В `docker-compose.yml` настроены лимиты:
- **CPU**: максимум 0.5 ядра, минимум 0.25 ядра
- **Память**: максимум 256 МБ, минимум 128 МБ

### Переменные окружения

- `BOT_TOKEN` - токен Telegram бота (обязательно)
- `IMAGE_TAG` - версия образа для docker-compose (по умолчанию: `latest`)

## 🔧 Разработка

### Сборка образа

Образ автоматически собирается при создании тега через GitHub Actions:
```bash
git tag v1.0.0
git push origin v1.0.0
```

### Локальная сборка

```bash
docker build -t pomodoro-bot .
docker run -e BOT_TOKEN=your_token pomodoro-bot
```

## 🛠 Технологии

- Python 3.8+
- aiogram 3.22.0
- python-dotenv 1.0.0
- Docker & Docker Compose
- GitHub Actions

## 📄 Лицензия

Этот проект распространяется под лицензией **Beer-ware**.

```
"THE BEER-WARE LICENSE" (Revision 42):
<author> wrote this file. As long as you retain this notice you
can do whatever you want with this stuff. If we meet some day, and you think
this stuff is worth it, you can buy me a beer in return.
```

Если вы встретите автора и считаете, что его работа стоит того, вы можете купить ему пиво 🍺
